                                            // IF, ELSE IF, ELSE//

    // the if statement. demonstration below.//


                        /*

            if (condition){
                code block to be executed
            }

                        */
/*
    let hour = 0800;
    if (hour > 0700){
        console.log("Good morning!")
    }

    this will display good morning on the console. it does this because the if statement is checking the variable 
        hour to see if it is more than 0700, in our case it is so the code block is executed displaying the
            message Good morning!
*/

// the if, else statment is shown below 

                        /*
                if(condition){
                    *code block to run if the condition is true*
                }
                else{
                    *code block to execute if the condition is false.*
                }
                        */

                        /*
                let hour = 1200;
                if(hour < 11.30){
                    console.log("Good morning!");
                }
                else{
                    console.log("Good afternoon!");
                }

                this of course will display good afternoon in the console. because the if statment checks the condition 
                    if the variable hour is less than 11:30 then the else code block will run.
                        */

// the if, else if, else//

                        /*
                        if(condition){
                            *code block to run if condition1 is true*
                        }
                        else if(condition2){
                            *code block to run if condition2 is true*
                        }
                        else{
                            *code block to run if condition1 and 2 return false.
                        }
                        */

                        /*
                        let hour = 2200;
                        if(hour < 1000){
                            console.log("Good morning");
                        }
                        else if(hour < 2000){
                            console.log("Good day.")
                        }
                        else{
                            console.log("good evening")
                        }


                        this checks three conditions it idnt quite acurate but works for demonstration purposes.
                
                        */

    